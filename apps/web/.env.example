# Formal Platform API (CORS should allow http://localhost:3000)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_PRINCIPAL_ID=dev-user
# Required when API_KEYS is set on the backend / in production:
# NEXT_PUBLIC_API_KEY=
# Force Lean-gated demo issuance in the UI:
NEXT_PUBLIC_REQUIRE_LEAN=false
