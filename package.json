{
  "name": "formal-platform",
  "private": true,
  "description": "Formal Platform monorepo — includes Next.js studio (apps/web)",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "build:web": "npm run build -w apps/web",
    "lint": "npm run lint -w apps/web"
  }
}
