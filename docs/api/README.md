# API documentation

OpenAPI is served by the FastAPI application at `/docs` when the API is running.

Initial routes (Phase 0/1):

- `GET /health`
- `GET /api/v1/meta`

Resource families (`/api/v1/organizations`, …) are introduced from Phase 2–3 onward.
